#!/bin/sh
# Pre-remove: stop and disable the service before files are removed.
set -e

case "$1" in
    remove|deconfigure)
        systemctl stop uc-chassis-robot.service || true
        systemctl disable uc-chassis-robot.service || true
        ;;
esac

exit 0
