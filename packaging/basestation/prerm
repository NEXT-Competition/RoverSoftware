#!/bin/sh
# Pre-remove: stop and disable the service before files are removed.
set -e

case "$1" in
    remove|deconfigure)
        systemctl stop roversoftware-basestation.service || true
        systemctl disable roversoftware-basestation.service || true
        ;;
esac

exit 0
